# אוטופאס – דף נחיתה למוסך (טסט שנתי ללא התעסקות)

דף נחיתה בעברית (RTL) ב-HTML / CSS / JS בלבד, ללא תלויות ובלי שרת.
כל ליד שנשלח מהטופס נכנס אוטומטית לטבלה בגוגל שיטס.

## קבצים

| קובץ | תפקיד |
|---|---|
| `index.html` | מבנה הדף: היירו, כאבי לקוח, תהליך, בדיקת 27 נקודות, השוואה, המלצות, שאלות נפוצות, טופס |
| `styles.css` | עיצוב מלא, רספונסיבי, כולל בר CTA קבוע במובייל |
| `script.js` | ולידציה, שליחה לגוגל שיט, אנימציות, מונים, FAQ, גיבוי מקומי |
| `google-apps-script.gs` | הקוד שרץ בצד גוגל וכותב את הליד לטבלה |

## הטבלה בגוגל שיטס

נמצאת בדרייב שלך, בתיקייה [CARWEB](https://drive.google.com/drive/folders/1Pw0dv-l1_2tKakhdHTeBCKmWpebLV2dk):
**[לידים - מוסך טסט שנתי (בדיקה חינם)](https://docs.google.com/spreadsheets/d/15LWO63T9VoghSyaJdNU_aYdaUSoOubS8sFGLSFcsHH4/edit)**

מזהה הגיליון (`SHEET_ID` ב-`google-apps-script.gs`) הוא `15LWO63T9VoghSyaJdNU_aYdaUSoOubS8sFGLSFcsHH4` והוא לא משתנה גם אם מעבירים את הקובץ בין תיקיות.

עמודות: חותמת זמן · שם מלא · טלפון · דגם / סוג רכב · חודש הטסט · מקור הליד · הערות · סטטוס · תאריך תיאום

> בשורה 2 יש ליד לדוגמה – למחוק אותה אחרי ההתקנה.
> הגיליון נוצר במקור עם עמודת "מספר רישוי"; הרצת `setupSheet` (שלב 3 למטה) מוחקת אותה אוטומטית.

## חיבור הטופס לטבלה (5 דקות)

1. פותחים את הגיליון → **תוספים / Extensions → Apps Script**.
2. מוחקים את הקוד הקיים ומדביקים את כל התוכן של `google-apps-script.gs`.
3. שומרים, ומריצים פעם אחת את הפונקציה `setupSheet` (מאשרים הרשאות בחלון שנפתח) – זה מעצב את הכותרות ומקפיא את שורה 1.
4. **Deploy → New deployment → Web app**
   - Execute as: **Me**
   - Who has access: **Anyone**
5. מעתיקים את הכתובת שמסתיימת ב-`/exec`.
6. פותחים את `script.js` ומדביקים אותה בשורה הראשונה:

```js
const CONFIG = {
  SHEET_ENDPOINT: 'https://script.google.com/macros/s/AKfy..../exec',
  SOURCE: 'דף נחיתה'
};
```

7. רוצים גם מייל על כל ליד? ב-`google-apps-script.gs` מלאו `NOTIFY_MAIL` ופרסמו מחדש (Deploy → Manage deployments → עריכה → New version).

**בדיקה:** פותחים את `index.html`, שולחים פנייה, ורואים שורה חדשה בגיליון.

> אם `SHEET_ENDPOINT` ריק – הטופס עדיין עובד ומציג הודעת הצלחה, והלידים נשמרים מקומית בדפדפן.
> אפשר לשלוף אותם בקונסול עם `exportLeads()` (מחזיר CSV).

## מה להחליף לפני העלאה לאוויר

| מה | איפה |
|---|---|
| שם המוסך "אוטופאס" | `index.html` – לוגו, כותרת, פוטר |
| טלפון `03-000-0000` | `index.html` – חיפוש והחלפה בכל הקובץ |
| וואטסאפ `972500000000` | `index.html` – שני קישורי `wa.me` |
| כתובת ושעות פעילות | סקשן `finalcta` בסוף `index.html` |
| מספרי הוכחה חברתית (11,000 רכבים, 4.9/5, 412 ביקורות) | `index.html` – היירו + `trustbar` |
| מחיר לדוגמה בדוח (420 ₪) | סקשן `report` ב-`index.html` |

⚠️ הנתונים הסטטיסטיים והביקורות בדף הם טקסט לדוגמה. יש להחליף בנתונים אמיתיים של המוסך לפני פרסום – פרסום נתוני שווא הוא הטעיה בפרסומת.

## העלאה לאוויר

הדף סטטי לחלוטין – אפשר להעלות כמו שהוא לכל אחסון:
Netlify (גרירה של התיקייה), Vercel, GitHub Pages, או תיקיית `public_html` בשרת רגיל.

## מדידה (אופציונלי)

`script.js` כבר יורה אירוע המרה אם מותקן פיקסל:
- Google Analytics / Ads → `gtag('event','generate_lead')`
- Meta Pixel → `fbq('track','Lead')`

מספיק להדביק את סקריפט הפיקסל ב-`<head>` של `index.html`.
